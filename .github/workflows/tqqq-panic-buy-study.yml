name: TQQQ Panic Buy Study
on:
  workflow_dispatch:
  push:
    paths:
      - 'research/tqqq_panic_buy_study_v4_dotcom.py'
      - '.github/workflows/tqqq-panic-buy-study.yml'
jobs:
  study:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pandas numpy yfinance numba
      - name: Run dotcom stress study
        run: PYTHONPATH=. python research/tqqq_panic_buy_study_v4_dotcom.py
      - uses: actions/upload-artifact@v4
        with:
          name: tqqq-panic-buy-v4-dotcom-results
          path: results/tqqq_panic_buy_v4_dotcom
